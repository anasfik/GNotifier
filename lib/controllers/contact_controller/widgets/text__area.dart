import 'package:flutter/material.dart';

class ContactTextArea extends StatelessWidget {
  const ContactTextArea({super.key});

  @override
  Widget build(BuildContext context) {
    return const TextField();
  }
}
